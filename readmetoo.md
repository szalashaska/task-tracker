# Install locally json server

npm i json-server

# Add script to package.json:

"server": "json-server --watch db.json --port 5000"

# run server on the terminal

npm run server

# to use routes u need to install:

npm i react-router-dom
